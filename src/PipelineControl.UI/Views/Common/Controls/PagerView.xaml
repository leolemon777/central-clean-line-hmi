<UserControl x:Class="PipelineControl.UI.Views.Common.Controls.PagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="PagerButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Width" Value="28" />
            <Setter Property="Height" Value="26" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="FontSize" Value="{StaticResource Font.Size.Meta}" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="Background" Value="{DynamicResource Bg.Card.Brush}" />
            <Setter Property="Foreground" Value="{DynamicResource Text.Secondary.Brush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Border.Strong.Brush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"
                                              Style="{StaticResource ButtonTemplateContentPresenterStyle}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.45" />
                    <Setter Property="Cursor" Value="Arrow" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="{Binding DisplaySummaryText, Mode=OneWay}"
                   VerticalAlignment="Center"
                   FontSize="{StaticResource Font.Size.Meta}"
                   Foreground="{DynamicResource Text.Secondary.Brush}" />

        <StackPanel Grid.Column="1"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">
            <Button Content="‹"
                    Command="{Binding PreviousPageCommand}"
                    Style="{StaticResource PagerButtonStyle}"
                    Margin="0,0,8,0" />
            <Border MinWidth="62"
                    Height="26"
                    Padding="10,0"
                    Background="{DynamicResource Bg.Page.Brush}"
                    BorderBrush="{DynamicResource Border.Subtle.Brush}"
                    BorderThickness="1"
                    CornerRadius="4">
                <TextBlock Text="{Binding PageSummaryText, Mode=OneWay}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontFamily="{StaticResource Font.Mono}"
                           FontSize="{StaticResource Font.Size.Small}"
                           Foreground="{DynamicResource Text.Secondary.Brush}" />
            </Border>
            <Button Content="›"
                    Command="{Binding NextPageCommand}"
                    Style="{StaticResource PagerButtonStyle}"
                    Margin="8,0,0,0" />
        </StackPanel>
    </Grid>
</UserControl>




