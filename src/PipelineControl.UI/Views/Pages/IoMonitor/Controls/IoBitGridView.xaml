<UserControl x:Class="PipelineControl.UI.Views.Pages.IoMonitor.Controls.IoBitGridView"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:PipelineControl.UI.Views.Pages.IoMonitor.Controls"
             xmlns:models="clr-namespace:PipelineControl.UI.ViewModels.Pages.IoMonitor.Models">
    <Border Background="{DynamicResource Bg.Page.Brush}"
            BorderBrush="{DynamicResource Border.Subtle.Brush}"
            BorderThickness="1"
            CornerRadius="3"
            Padding="4,3">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="{Binding ModuleLabel, ElementName=Root, Mode=OneWay}"
                               FontFamily="{StaticResource Font.Chinese}"
                               FontSize="{StaticResource Font.Size.Small}"
                               FontWeight="Normal"
                               Foreground="{DynamicResource Text.Primary.Brush}"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis" />
                </StackPanel>
                <Border Grid.Column="1"
                        Background="{DynamicResource Bg.Card.Brush}"
                        BorderBrush="{DynamicResource Border.Subtle.Brush}"
                        BorderThickness="1"
                        CornerRadius="3"
                        Padding="5,1"
                        VerticalAlignment="Center">
                    <TextBlock Text="{Binding RegisterValueHex, ElementName=Root, Mode=OneWay}"
                               FontFamily="{StaticResource Font.Mono}"
                               FontSize="{StaticResource Font.Size.Micro}"
                               Foreground="{DynamicResource Text.Secondary.Brush}" />
                </Border>
            </Grid>

            <ItemsControl Grid.Row="1"
                          ItemsSource="{Binding Points, ElementName=Root}"
                          Tag="{Binding Columns, ElementName=Root}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="{Binding Tag, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="{x:Type ContentPresenter}">
                        <Setter Property="Margin" Value="1" />
                        <Setter Property="VerticalAlignment" Value="Stretch" />
                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:IoPointViewModel}">
                        <controls:IoBitCellView Tag="{Binding}"
                                                BitText="{Binding BitText}"
                                                SignalText="{Binding CellSignalText}"
                                                IsOn="{Binding IsOn}"
                                                IsOutput="{Binding IsOutput}"
                                                IsForced="{Binding IsForced}"
                                                IsSelected="{Binding IsSelected}"
                                                ShowBitText="{Binding ShowBitText, Mode=OneWay}"
                                                CellTooltip="{Binding TooltipText}"
                                                ClickCommand="{Binding SelectionChangedCommand, RelativeSource={RelativeSource AncestorType={x:Type controls:IoBitGridView}}}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>
