<UserControl x:Class="PipelineControl.UI.Views.Shell.PlaceholderPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{DynamicResource Bg.Page.Brush}">
        <Border Background="{DynamicResource Bg.Card.Brush}"
                BorderBrush="{DynamicResource Border.Default.Brush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="28"
                Width="560"
                HorizontalAlignment="Left"
                Margin="0,20,0,0">
            <StackPanel>
                <TextBlock Text="页面承载区 · 骨架模式"
                           FontFamily="{StaticResource Font.Sans}"
                           FontSize="{StaticResource Font.Size.Title}"
                           FontWeight="Normal"
                           Foreground="{DynamicResource Text.Primary.Brush}" />
                <TextBlock Text="当前仅保留主框架，后续页面将按步骤逐步接入。"
                           FontSize="{StaticResource Font.Size.Body}"
                           Foreground="{DynamicResource Text.Secondary.Brush}"
                           Margin="0,8,0,12" />
                <TextBlock Text="{Binding Path=., Mode=OneWay}"
                           FontSize="{StaticResource Font.Size.Small}"
                           Foreground="{DynamicResource Text.Tertiary.Brush}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>



